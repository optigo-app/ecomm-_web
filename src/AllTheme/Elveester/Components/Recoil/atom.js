import { atom } from "recoil";

export const el_companyLogo = atom({
    key:'dt_companyLogo',
    default:[]
  })

  export const el_loginState = atom({
    key: 'dt_loginState',
    default: false,
  })