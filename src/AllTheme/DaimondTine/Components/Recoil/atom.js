import { atom } from "recoil";

export const dt_companyLogo = atom({
    key:'dt_companyLogo',
    default:[]
  })

  export const dt_loginState = atom({
    key: 'dt_loginState',
    default: false,
  })